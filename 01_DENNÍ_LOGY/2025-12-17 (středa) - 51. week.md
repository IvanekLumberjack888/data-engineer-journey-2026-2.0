Další příprava:

# Praktická příprava (QUIZ)

ZPOČÁTKU STŘEDNÍ OBTÍŽNOST PAK PŘITVRZENÍ.

	 ❓V jakém scénáři je nejvhodnější použít vlastní Spark Pool (Custom Spark Pool) namísto výchozího Starter Poolu v Microsoft Fabric?

		Pokud půjde o trénování ML modelu.
		Je třeba navýšit jak uzly (Node size).
		A je potřeba více paměti/GPU. 🔝 OPTIMIZED

	❓Které z následujících nastavení se NIKDY neukládá do Git repozitáře při zapnuté správě verzí (Version Control) pro Fabric workspace?

		 Plány automatického obnobování dat (Refresh schedules) pro sémantický model.

	❓Datový inženýr nasazuje změny z vývojového (DEV) do testovacího (TEST) prostředí pomocí Deployment Pipelines. Po nasazení zjistí, že data v tabulkách Data Warehouse v TEST prostředí chybí. Co je nejpravděpodobnější příčinou?

		Přece Deployment Pipelines přenáší pouze definice a schémata objektů, nikoliv samotná data v tabulkách.

	❓Jaký je hlavní rozdíl mezi oprávněními 'ReadData' a 'ReadAll' při sdílení Data Warehouse na úrovni položky (item-level sharing)?


		'ReadData' umožňuje čtení dat pouze přes T-SQL endpoint, zatímco 'ReadAll' přidává možnost čtení podkladových souborů v OneLake (např. pro Spark)

	❓Jaký je hlavní rozdíl mezi orches­tračním nástrojem Data Pipeline a orches­trací pomocí Fabric Notebooku?

		Data-pipeline nabízí vizuální low-code rozhraní, zatímco orchestrace v notebooku je code-first přístup v Pythonu.

	❓Vytváříte datovou pipeline, která má načítat denní přírůstky dat z relační databáze do Delta tabulky v Lakehouse. Zdrojová tabulka má sloupec `LastModifiedDate`. Jaký přístup zvolíte pro efektivní načítání?

		 Easy: Incremental load s použitím 'MERGE' operace v PySparku, která porovná záznamy podle 'LastModifiedDate'

	❓Při implementaci dimenzionálního modelu chcete zachovat kompletní historii změn v atributech zákazníka (např. změna adresy). Který typ Slowly Changing Dimension (SCD) a jaký klíč musíte použít?

		SCD Type 2 s použitím surrogate (náhradního) klíče jako primárního klíče.

	 ❓V jaké situaci je vhodnější použít Dataflow Gen2 namísto Copy aktivity v Data Pipeline pro transformaci dat?

		Když transformaci provádí business analytik, který je zvyklý na rozhraní Power Query a potřebuje data profilovat a čistit pomocí více něž 300 vestavěných funkcí.

	❓Jaký je zásadní rozdíl mezi použitím Shortcuts a Mirroring pro integraci externích dat do Fabricu?

		Mirroring replikuje data do Fabriku pro analytické účely s nízkou latencí. Zatímco Shortcuts pouze vytváří logický odkaz bez kopírování dat.

	❓V Eventstreamu potřebujete spočítat průměrnou teplotu za každou uplynulou hodinu, přičemž výpočet se má provést vždy po skončení hodinové periody. Který typ okenní funkce v transformaci 'Group By' je pro tento účel nejvhodnější?

		Tumbling window - rozděluje datový proud na pevné - po sobě jdoucí nepřekrývající se intervaly. Ideální pro periodické reporty jako průmer/hodina.