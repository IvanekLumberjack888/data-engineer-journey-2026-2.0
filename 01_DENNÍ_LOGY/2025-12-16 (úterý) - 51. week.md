# Praktická příprava (QUIZ)

	V rámci optimalizace výkonu čtení v Lakehouse pro sémantický model v režimu Direct Lake je potřeba zajistit, aby data byla uložena ve formátu V-Order. Který z následujících způsobů konfigurace V-Order zajistí, že VŠECHNY budoucí zápisy do konkrétní tabulky `FactSales` budou VŽDY používat V-Order, bez ohledu na nastavení Spark session v notebooku, který zápis provádí?

		👓 Spuštění příkazu ALTER TABLE 👀
	
	 Analytik sleduje výkon KQL dotazů v Eventhouse a všimne si, že dotaz, který hledá chybové zprávy, je výrazně pomalý. Dotaz vypadá takto: `Logs | where Timestamp > ago(7d) and Message contains "error"`. Jaká optimalizace by s největší pravděpodobností přinesla největší zrychlení tohoto dotazu?

		👓 Změním operátor 'contains' na 'has' 👀
		Protože contains to prohledává i podřetězce.
		 Tím pádem je has rychlejší. 🪓

	 Společnost chce delegovat správu pracovních prostorů patřících do oddělení 'Finance' na vedoucího tohoto oddělení, ale zároveň si chce centrální IT (Fabric Admin) ponechat plnou kontrolu nad celým tenantem. Jaká kombinace rolí a konceptů v Fabric toto nejlépe umožňuje?

		To bylo lehký: Doména Finance a Domain Admin bude CFO

	 Při použití OneLake zkratek (shortcuts) na data v Amazon S3 je aktivováno kešování pro zlepšení výkonu. Soubor `transactions_2025-12-10.parquet` o velikosti 500 MB byl načten a uložen do keše. Co se stane, pokud k tomuto souboru nikdo nepřistoupí po dobu 30 hodin a poté je znovu dotázán?

		Taky easy: Uplynulo 24h, takže znova načíst z Amazon S3 a
		 znovu ten soubor bude uložen do cache.

	 Data Pipeline v Fabric obsahuje aktivitu Notebook, která selhává. Kde by měl data engineer jako první hledat podrobnou chybovou zprávu a stav běhu jednotlivých Spark úloh spuštěných tímto notebookem?

		 Taky easy: Monitoring Hub. Detali se rozklikne a
		 vídíme pak detail selhání a pak víc ve Spark History Server

	 Během streamování dat pomocí Eventstreamu do Lakehouse tabulky si všimnete v `Data Insights`, že metrika `IncomingMessages` ukazuje stabilní tok 1000 zpráv za minutu, ale metrika `OutgoingMessages` je trvale na nule. V `Runtime logs` nejsou žádné chybové hlášky. Co je nejpravděpodobnější příčinou tohoto chování?

		To znamená, že v Eventstreamu je 'drsný filtr' proto 🅾️

	 Tým vyvíjí streamovací řešení pro analýzu uživatelských seancí na webu. Cílem je pro každého uživatele agregovat počet kliknutí v rámci jedné návštěvy. Návštěva je definována jako sekvence akcí, která je ukončena, pokud uživatel nevykazuje žádnou aktivitu po dobu 30 minut. Který typ okenní funkce v Eventstreams je pro tento scénář nejvhodnější?

		Hopping/Session/Sliding/Tumbling? hadej PEPO
		 🎯SESSION s timeouten neaktivity na 30 sekund

	 Dataflow Gen2, které provádí komplexní transformace (několik spojení a agregací) na 20 milionech řádků z Azure SQL databáze, běží velmi pomalu. Jaká optimalizační technika by s největší pravděpodobností přinesla nejvýznamnější zrychlení?

		To je jasný: POVOLIT (Enable Staging) stejdžing :D

	